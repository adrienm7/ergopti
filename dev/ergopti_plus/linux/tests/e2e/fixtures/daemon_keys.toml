# tests/e2e/fixtures/daemon_keys.toml
#
# The catalogue the daemon-level scenarios type against. Pinned here rather
# than read from the bundled packs, whose sections ship disabled and change.

[[scenarios]]
"adn" = { output = "ADN", is_word = true, auto_expand = false, is_case_sensitive = true, final_result = false }
"pk★" = { output = "parce que", is_word = false, auto_expand = true, is_case_sensitive = false, final_result = false }

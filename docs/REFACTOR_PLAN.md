# ErgoptiPlus — Plan de simplification & mise en commun (pur refactor)

> **But.** Faire converger les drivers `windows/` (AHK), `macos/` (Hammerspoon/Lua) et
> `linux/` vers **une seule structure miroir**, pousser le **maximum de logique/config
> dans `_shared/`**, et rendre le code assez simple pour qu'un junior comprenne un driver
> en quelques heures. **Aucun changement de comportement** : chaque étape est un refactor
> vérifié.
>
> Ce document remplace les anciens rapports d'audit racine (`AUDIT_*`, `RAPPORT_AUDIT*`)
> qui ont servi à le construire. On coche au fur et à mesure.

## Comment on travaille

1. On exécute les phases **dans l'ordre** (`P0` → `P7`), de la moins risquée à la plus risquée.
2. Chaque étape est cochée **uniquement après** sa vérification.
3. Chaque phase est un (ou plusieurs) commit conventionnel sur `dev`.
4. **Règle d'or** (CLAUDE.md §5.9) : tout fix embarque son test de régression dans le même commit.

> **Décision mainteneur 2026-06-23 — on termine TOUT le reste.** Tous les items
> encore ouverts (y compris ceux marqués *REPORTÉ* / *optionnel* / faible gain)
> sont désormais à faire, dans cet ordre explicite : **(1)** leftovers P2/P3,
> **(2)** P6 restant (`scan personal-hotstrings` + `file-watchers` d'`init.lua`,
> puis `api_mlx.lua`), **(3)** items optionnels P4/P5 + passe `fix:banners`,
> **(4)** P7 en dernier, sous-étape par sous-étape. Discipline inchangée : chaque
> incrément vérifié (dry-run AHK / `test:hs` / `test:js` / lint / encoding) avant
> commit ; ce qui n'est vérifiable qu'au reload (boot Hammerspoon, GUI Windows)
> est signalé au mainteneur.

### Harnais de vérification (établi, baseline verte le 2026-06-20)

| Cible | Commande | Sens |
|---|---|---|
| Parse AHK (include graph) | `& "C:\Program Files\AutoHotkey\v2.0.19\AutoHotkey64.exe" /ErrorStdOut <runner> --dry-run` | exit 0 = tout parse (`1..2208` au baseline) |
| Tests unitaires AHK | idem sans `--dry-run` | runner = `static/ergopti_plus/windows/tests/run_all.ahk` |
| E2E AHK | runner = `static/ergopti_plus/windows/tests/e2e/run_e2e.ahk` | |
| Suite node | `npm run test:manifest-parity test:port-compliance test:ahk-encoding test:manifest-equivalence test:config-schema test:hs-integrity` (+ autres `test:*`) | |
| Lua (macOS, stubs) | `lua static/ergopti_plus/macos/tests/run.lua` | |

---

## Structure cible (miroir 1:1)

Chemin identique d'un driver à l'autre → l'analogue se trouve au même endroit.

| Concept | `windows/` | `macos/` | Rôle |
|---|---|---|---|
| Entrée | `ErgoptiPlus.ahk` (mince) | `init.lua` (mince) | directives + manifeste d'include + filet d'erreur + `Boot_Run()` |
| Adapters | `adapters/` (20 exactement) | `adapters/` (20 exactement) | 1 fichier / port de `_shared/core/ports/contracts.json` |
| Infra/domaine | `lib/` (aucun UI) | `lib/` (aucun UI) | |
| Features | `modules/<feature>/` | `modules/<feature>/` | 1 dossier / feature |
| Fenêtres UI | `ui/<window>/` | `ui/<window>/` | 1 dossier / fenêtre |
| Données | `data/` (data pure) | `data/` (data pure) | |
| Généré | `_generated/` | `_generated/` | jamais édité à la main |
| Tests | `tests/{unit,integration,e2e,bench,fixtures,helpers,stubs,meta}` | idem | même taxonomie |

---

## P0 — Hygiène repo (zéro impact code)

**But.** Nettoyer le tracking git et la racine pour que la structure soit lisible avant tout déplacement de code.

- [x] Archiver les 8 audits markdown uniques (`AUDIT_*.md`, `RAPPORT_AUDIT*.md`) sous `docs/archive/audits/` ; supprimer `audit_ergoptiplus.md` (doublon byte-identique de `RAPPORT_AUDIT_FINAL.md`, md5 vérifié).
- [x] `git rm` les dumps git-log jetables `all_fix_commits.txt`, `fix_commits.txt`.
- [x] `git rm --cached static/ergopti_plus/windows/build/static_bundle.zip` (déjà gitignore l.153, reste sur disque, CI le reconstruit).
- [x] `git rm --cached reports/mutation/mutation.html` + ajouter `/reports/` à `.gitignore` (l.132).
- [x] `git rm static/ergopti_plus/kanata/{kanata,kanata_winIOv2.exe}` (binaires non référencés ; `linux/install.sh` n'utilise que `kanata.kbd` — vérifié). Garder `kanata.kbd`.
- [x] Supprimer du working tree les logs scratch non trackés (`ci.log`, `failed_run.log`, `windows/out.log`, `tests/test_run.log`, `macos/tests/scratch_*.log`, `luac.out`).
- [x] `luac.out`/`*.luac` déjà couverts par `.gitignore` (l.65-66) — vérifié.

**Vérif. ✅** `git check-ignore` confirme zip + `reports/` ignorés ; `test:ahk-encoding` (571 fichiers) + `test:config-schema` verts ; aucun fichier tracké cassé.

---

## P1 — Drift gate codegen + accesseur feature fail-fast (tue la classe de crash)

**But.** Rendre le manifest source unique **imposée**, et transformer l'`UnsetItemError` opaque (crash `ctrl_magic_save`, [layout.ahk:743]) en erreur nommée et actionnable.

- [x] Régénérer + commiter `windows/_generated/config_template.toml` (périmé — prouvé) — commit séparé `fix(codegen): sync config_template + schema`. A aussi révélé que `config.schema.json` (hand-maintained) rejetait `ctrl_magic_save` → corrigé.
- [x] Script `codegen` parapluie ajouté dans `package.json` (= `npm run build:domain`, qui régénère + vérifie).
- [x] Drift gate CI : **existe déjà** dans `build:domain` (drift-check sur 8 fichiers générés + `contracts.json` via port-compliance), **et la CI le lance déjà** (`ci.yml:102`). Vérifié : il a bien attrapé le `config_template` périmé.
- [x] **Nouveau** check statique `tools/test/test-feature-read-sites.js` : tout site de lecture `Features[...]` AHK (279 sites) résout contre la map construite depuis le manifest — couvre les flags ahk-only. Câblé dans `build:domain` (step 10) → tourne en CI.
- [x] Régression : self-test **toujours actif** dans le guard, encode la classe de crash exacte — forme buggée `Features["ahk.layout"]["ctrl_magic_save"]` **rejetée**, forme correcte `Features["layout"][...]` **acceptée**, clé inconnue **rejetée**.
- [x] `test-manifest-parity.cjs` laissé tel quel : son exclusion des flags `platforms=['ahk']` est **correcte** (la parité est cross-driver ; un flag ahk-only n'existe pas côté HS). Le trou réel — l'absence de contrôle interne des flags ahk-only — est désormais comblé par le guard ci-dessus.
- [ ] ~~Accesseur AHK `FeatureEnabled/FeatureValue/FeatureSet` + conversion des sites~~ → **REPORTÉ en P4**. Raison : il y a **294** sites `Features[...]` (pas 31), beaucoup dans des `#HotIf ... IsSet(Features)` — une conversion de masse à l'aveugle (sans lancer le driver GUI) est risquée, et ajouter un accesseur dormant violerait §5.6 (pas de code mort). Le guard statique atteint l'objectif anti-crash sans toucher au runtime. La migration vers un accesseur fail-fast (lisant la **map live**) se fera dans P4 où l'entrée est réécrite et vérifiable.

**Vérif. ✅** `build:domain` 10/10 vert (guard inclus) ; `config_template` + schéma synchronisés ; suite node verte ; le guard distingue précisément la forme buggée de la forme correcte.

---

## P2 — Tests miroir + auto-découverte (CI lisible)

**But.** Un point d'entrée unique, des échecs lisibles, ajouter un test sans éditer le runner.

- [x] Scripts `package.json` : `test:js` (umbrella node, mirror des jobs "Validate ·"), `test:hs`, `test:linux`. `test:ahk` documenté dans `docs/TESTING.md` (chemin de l'exe AHK variable → laissé en doc plutôt qu'en script hardcodé).
- [x] **Point d'entrée + échecs lisibles** : `tools/test/run-js-suite.cjs` affiche un résumé pass/fail par check et, sur échec, **la commande exacte pour rejouer** + le tail de sortie. `docs/TESTING.md` = doc unique « comment lancer/diagnostiquer chaque couche, local == CI ».
- [x] **Format TAP+JSON unifié + `report.cjs` + annotations GitHub** ✅ (2026-06-23) — `tools/test/report.cjs` enrobe un runner, streame sa sortie inchangée, sort avec son code (gating CI préservé) et parse les DEUX formats (TAP de `run_all.ahk` + format Lua `ok/FAIL` + footers). Émet `::error::`/`::notice::` + résumé d'étape, écrit un JSON ; mode `PARSE_FILE` pour le fichier de résultats AHK. Self-test `test-report.cjs` câblé dans `test:js` (14 checks). CI : étape Lua macOS enrobée ; post-étape `Annotate AHK results` (continue-on-error) parse le TAP AHK. Additif — gating inchangé.
- [x] **Taxonomie de tests miroir sur Windows** ✅ (2026-06-23) — les **78 fichiers de test plats** `windows/tests/test_*.ahk` déplacés dans `tests/unit/` (miroir de macOS `tests/{unit,meta,e2e,…}`) ; `test_framework.ahk` + `test_stubs.ahk` (infra) restent à la racine `tests/`. 83 `#Include` réécrits (run_all + runners standalone), en-têtes de chemin mis à jour, chemins relatifs exposés par le déplacement corrigés (`test_shortcuts` `../`→`../../` ; dérivation racine `A_LineFile` de `test_audit_v4/v5` ; lecture du tint meta-test). Drift gate scanne `tests/unit/`. Vérifié : dry-run exit 0, suite 2441/0, encoding 621, test:js 14/14.
- [x] **Auto-découverte `run_all.ahk`** ✅ (réalisée comme *drift gate*, pas comme manifeste codegen — 2026-06-23). AHK résout `#Include` à la compilation : un `test_*.ahk` que personne n'inclut est silencieusement sauté. **16 fichiers de test étaient orphelins** (jamais inclus → 0 couverture, certains depuis P5). Résolus : **13 recâblés** dans `run_all.ahk` (dont 6 « test bugs » corrigés — chemin `A_ScriptDir` faux, 1er appel vs définition, forme deadline vs elapsed, commentaire piégeant un `InStr`, `InStr` param #3 invalide ; le code prod était déjà correct), **3 supprimés** (morts depuis P5, `#Include` de fichiers déplacés) avec leur couverture unique préservée (formatters healthcheck → nouveau `meta/test_healthcheck_format_helpers.ahk` + `ui/healthcheck/helpers.ahk` headless-safe inclus). Nouveau garde-fou `tools/test/test-ahk-test-coverage.cjs` (câblé dans `test:js`, 13 checks) : marche la clôture `#Include` depuis `run_all.ahk` et échoue si un `test_*.ahk` n'est pas atteignable → l'orphelinage ne peut plus se reproduire. Un manifeste codegen aurait dû réordonner 245 includes dépendance-ordonnés (haut risque, gain marginal) ; le garde-fou est complet (423 fichiers) sans ce risque. Suite AHK 2408 → 2441.

**Vérif. ✅** `test:js` 4/4 vert ; `test:hs` 2228 tests verts ; doc TESTING.md couvre les 4 couches avec la commande exacte de chacune.

---

## P3 — Parité adapters (structurel bas risque)

- [x] ~~Déplacer `macos/adapters/{json_codec,shell_runner,toml_cache}.lua` → `macos/lib/`~~ → **REJETÉ** par l'architecture. Le test `tests/meta/test_port_adapter_coverage.lua` impose que tout appel OS (`hs.*`, `io.open`/`os.execute`) vive dans `adapters/`. Ces 3 helpers **touchent l'OS** (exec shell, I/O fichier) → ils sont légitimement dans `adapters/`. Affaiblir le baseline du test pour permettre le move est interdit (§5.9). **Conclusion : `adapters/` = couche d'isolation OS (les 20 ports + helpers OS), pas « exactement 20 ports ».** La parité visée doit être documentée ainsi, pas forcée par un déplacement. Voir [PROJECT_MEMORY](PROJECT_MEMORY.md).
- [x] **Maps `ADAPTER_<NAME>` + durcir `test-port-compliance`** ✅ (2026-06-23) — 7 des 20 adaptateurs n'avaient PAS de map `global ADAPTER_<NAME> := Map(...)` ; le test ne validait donc que les 13 présents (une map supprimée passait en silence). Maps ajoutées (crypto, graphics_renderer, keyboard_hook, mouse_control, network_info, window_manager, tooltip_renderer), chacune mappant les méthodes de contrat aux fonctions existantes. Garde-fou de **complétude** ajouté : tout port dont le fichier adaptateur existe DOIT déclarer sa map (a immédiatement attrapé `tooltip_renderer.ahk`). 22/22 checks. Suite 2441/0.
- [x] **Normaliser en-têtes** ✅ — déjà cohérents (`; adapters/<name>.ahk` + bannière + `; MODULE: <Name> Adapter (AutoHotkey)`) sur tous les adaptateurs ; rien à faire. **Codegen vecteurs de contrat → écarté (net négatif, prouvé)** : le test AHK `test_adapter_contract_vectors.ahk` est une **traduction manuelle** qui ne préserve pas les `id` des specs (22/138 seulement) → pas de drift-gate bon-marché ; un vrai codegen JS→AHK serait une réécriture risquée d'un test fonctionnel de 1000+ lignes pour une valeur marginale (contrats stables). Headers faits, le reste est disproportionné.

**Vérif. ✅** Le filet de test (`test:hs`, 2228 tests) a rejeté le move ; revert propre, suite re-verte. P3 : aucun changement de code — le principal item était architecturalement incorrect.

---

## P4 — Décomposition de l'entrée Windows

**But.** `ErgoptiPlus.ahk` 2397 → ~150 lignes, miroir de `init.lua`.

**Pipeline d'extraction prouvé et sûr** (chaque incrément) : extraction PowerShell (garantit UTF-8 BOM + CRLF) qui remplace le bloc **en place** par un `#Include` (préserve l'ordre de boot et les statements top-level type `global X := …`) → `test:ahk-encoding` → dry-run AHK (parse, exit 0) → suite complète AHK (2224/0) → repointer les meta-tests qui scannent le source de `ErgoptiPlus.ahk` (≈25 tests pinnent des fonctions à ce fichier — le filet les attrape).

- [x] Hotkeys AltGr → `lib/script_altgr_hotkeys.ahk` (incrément 1 ; 2 meta-tests repointés).
- [x] Éditeurs (magic key, repeat key, infos perso, lien GPT) → `ui/editors.ahk` (incrément 2).
- [x] Pickers (`ShowActionPicker`, `ShowKeyboardSlotPicker`, `ShowKeyboardShortcutPicker`, `FilePathsEditor`) → `ui/action_picker.ahk` (incrément 3).
- [x] Config IO (toggles, `SaveFullConfig`, `_CollectFeatureUpdates`, shortcut config) → `lib/config_io.ahk` (incrément 4). **Bonus** : durcissement des meta-tests d'introspection — `_DriverSourceConcat()` (tout l'arbre `windows/**/*.ahk`) + `_DriverFuncBody()` (ancre sur la *définition*, indentée ou non) dans `test_framework.ahk` ; ~28 tests migrés → **désormais indépendants de l'emplacement des fonctions** (plus de casse à chaque déplacement).
- [x] Lifecycle (suspend/shutdown/tray/debug) → `lib/lifecycle.ahk` (incrément 5).
- [x] Tables d'état globales (`ScriptInformation`, `SCRIPT_SHORTCUT_*`, `KEYBOARD_SHORTCUT_*`, `CategoryEnabled` + readers) → `lib/feature_state.ahk` (incrément 6 ; in-place ; 0 fix de test grâce au durcissement).
- [x] Sondes `DllCall` (AltGr-Kana, scan magic-key) → `adapters/key_state.ahk` ✅ ; bloc d'init de boot → `lib/boot.ahk` ✅.
  - [x] **Sondes layout → `adapters/key_state.ahk`** ✅ — les 5 `DllCall` de détection de layout au boot (`GetKeyboardLayout`/`SystemParametersInfo`/`MapVirtualKeyExW`×2/`ToUnicodeEx`), inlinés en 2 endroits de l'entrée (bloc de log AltGr reverse-probe + scan scancode magic-key) avec la cascade HKL 3-étapes **dupliquée**, extraites en 3 helpers Windows-only : `KS_ResolveKeyboardLayout` (cascade dédupliquée), `KS_ProbeRightAltScancode` (VK_RMENU→SC), `KS_ScanScancodeForChar` (scan `ToUnicodeEx` sans modificateur, `wFlags=0x4` dead-key-safe). L'entrée ne fait plus qu'appeler + interpréter. **Pas** ajoutées au port map cross-platform `KeyState` (isDown/isUp) — ce sont des sondes Win32-spécifiques, donc zéro changement de spec/miroir macOS. Relocation pure (même séquence de `DllCall`, même ordre → comportement identique sur tous les layouts). Les 2 gardes source whole-tree (altgr-detect HKL fallback, magic-key `wFlags=0x4`) restent vertes ; +3 smoke-tests callable. Dry-run exit 0, suite **2444/0**, encoding 621, lint vert. **⏳ En attente de reload-test Windows par le mainteneur (AltGr, magic-key sur layout non-Ergopti, logs de boot).**
  - [x] **Bloc d'init de boot → `lib/boot.ahk`** ✅ — le bloc top-level d'initialisation (globals + chargement config/timings/hotstrings précoce + icône tray + bootstrap du dossier de config, ex-lignes 264-382) déplacé dans `lib/boot.ahk` et réinjecté par **un seul `#Include` à la ligne exacte qu'il occupait**. AHK exécute le top-level dans l'ordre des `#Include`, donc un include en place préserve l'ordre auto-execute **à l'octet** (même technique prouvée que `lib/feature_state.ahk`). Bloc **pur init** : aucune directive `#HotIf`/`#InputLevel`, aucun hotkey → aucun scope de directive perturbé. Entrée **920 → 802 l.**. Les 3 gardes source de boot (DirCreate try-wrap, FileAppend bootstrap try-wrap, `_ReleasePhantomModifiers`) restent vertes (scan whole-tree → trouvent le contenu dans `boot.ahk`). Dry-run exit 0, suite **2446/0**, encoding 622, lint vert. **⏳ En attente de reload-test Windows (boot normal : icône tray, chargement config/hotstrings, pas de dialogue d'erreur).** Le reste de l'entrée (manifeste `#Include` + orchestration de boot interleavée) est le rôle légitime de l'entrée — pas d'extraction supplémentaire prévue.
- [x] Bannières 5-blank-lines / 7-`=` dans les nouveaux fichiers ✅ — passe `npm run fix:all` (`--fix-banners --fix-unbalanced --fix-spacing`) **no-op** : 0 fichier modifié, `lint-conventions` whole-tree vert (594 AHK + 482 Lua + 11 TOML). Les nouveaux fichiers ont été alignés au fil de l'eau à chaque incrément.

**Vérif. ✅** Driver rechargé par le mainteneur — fonctionne comme avant. Chaque incrément : dry-run exit 0 + suite **2230/0** + encoding vert. **Entrée : 2397 → 1189 lignes (−50 %).** Cœur de P4 atteint ; le reste de l'entrée est manifeste + boot.

---

## P5 — Folderisation `modules/` + `ui/` Windows

- [x] `tray_menu.ahk` (2975 l.) → **`ui/menu/` (12 fichiers)** : `menu_engine`, `menu_gestures`, `menu_metrics`, `menu_metrics_actions`, `menu_layout`, `menu_hotstrings`, `menu_submenus`, `menu_shortcuts`, `menu_taphold`, `menu_init`, `menu_actions`, `menu_rebuild`. `tray_menu.ahk` = index de 95 l. (header + globals d'état + `#Include` in-place → ordre d'exécution top-level préservé à l'octet). 5 meta-tests file-pinnés migrés vers `_DriverSourceConcat()`/`_DriverFuncBody()`. **Vérif** : dry-run exit 0, suite 2230/0, encoding 589 fichiers.
- [x] `ui/tray_llm/` (10 fichiers) → **`ui/menu/menu_llm/`** (miroir macOS exact, plus profond que le `ui/menu_llm/` plat initialement prévu) + rename complet de la famille d'identifiants `LLM_Tray_*`/`_LLM_Tray*`/`LLM_TRAY_*` → `LLM_Menu_*`/`_LLM_Menu*`/`LLM_MENU_*` (**smart mapping** : `_LLM_Tray_Menu`→`_LLM_Menu_Handle` pour éviter `_Menu_Menu`, et `_LLM_Tray_InTray`→`_LLM_Menu_InTray` qui garde le sens « barre système »). 6 fichiers de test renommés (`test_llm_tray_*`→`test_llm_menu_*` + `test_tray_llm_actions_include`→`test_menu_llm_actions_include`) ; commentaires de parité macOS (`api_panel`, `profiles_manager`, `profile_label`, 2 tests) mis à jour. **1203 remplacements sur 50 fichiers**, 0 résiduel. **Vérif** : `/validate` exit 0 (entry + run_all), suite AHK 2382/0, suite Lua macOS 2317/0. Reload-test Windows en attente.
- [x] `lib/tooltip.ahk` (2251 l.) → **`ui/tooltip/{init,core,helpers,llm}.ahk`** (move lib→ui + split, miroir macOS). `init.ahk` = index (module doc + 3 `#Include`). Includes mis à jour : `ErgoptiPlus.ahk` + `tests/run_all.ahk`. Nouveau helper `_DriverDirConcat("ui/tooltip")` dans `test_framework.ahk` (scan source par module, indépendant du sous-découpage) ; 7 tests d'introspection migrés. Orphelin non-CI rotté `run_llm_tooltip_grace.ahk` supprimé (déjà rouge sur un contrat de garde périmé ; superseded par `test_llm_tooltip_grace.ahk` dans run_all). **Vérif** : dry-run exit 0, suite 2228/0, encoding 591.
- [x] `lib/updater.ahk` (1838 l.) → **`lib/updater/{core,changelog,self_update}.ahk`** (split en place ; `updater.ahk` reste l'index dans `lib/`, aucun changement d'`#Include`). Nouveau helper `_DriverDirConcat` réutilisé ; **7 fichiers de tests d'introspection migrés** (16 assertions) vers `_DriverDirConcat("lib/updater")`/`_DriverFuncBody` ; `test_webview2_temp_leak` passe en scope module (create + cleanup `_Updater_CloseGui` désormais dans des sous-fichiers frères). **Vérif** : dry-run exit 0, suite 2234/0, encoding 594.
- [x] `lib/healthcheck.ahk` (956 l.) → **`ui/healthcheck/{init,core,helpers}.ahk`** (move lib→ui, miroir macOS ; module UI). `init.ahk` = index. 4 tests d'introspection migrés vers `_DriverDirConcat("ui/healthcheck")`/`_DriverFuncBody`. **Vérif** : dry-run exit 0, suite 2232/0, encoding 596.
- [x] `modules/gestures.ahk` (2076 l.) → **4 sous-modules** (`gestures_click.ahk`, `gestures_screenshots.ahk`, `gestures_window_cycle.ahk`, `gestures_config.ahk`) + l'index `gestures.ahk` réduit à **1289 l. (−38 %)**. Seules des fonctions migrent : en AHK elles restent dans le namespace global (résolues au load à travers les `#Include`), donc le catalogue, les closures, les hotkeys top-level et l'install du hook WinEvent restent **dans l'index** à leur position exacte → zéro changement d'ordre boot/`#InputLevel`. Chaque sous-module `#Include`é après `gestures.ahk` dans les 2 entrées. Meta-tests d'introspection déjà move-resilient (`_DriverFuncBody`) ; 2 tests file-pinnés repointés (`test_audit_v4_fixes`, `test_actions_snap_rclick` côté macOS). **Reload-test live OK** (clic-maintenu, captures, cycle de fenêtres, picker d'actions). Au passage **2 bugs pré-existants corrigés + tests de régression** : relâchement du clic-maintenu au tap 1-doigt (`fb38649e6`), et `SetTimer(_GestureLoadActionCatalog, -0)` (no-op en AHK v2 : `-0`==`0` désactive le timer) qui laissait le picker d'actions **vide** → `-1` (`d8f99c8cb`). **Vérif** : suite AHK 2376/0, encoding 608, JS 12/12, lint vert.
  - *Note miroir macOS (même session)* : god-files macOS aussi dégraissés — `gestures/actions.lua`→`actions_click.lua`, `gestures/engine.lua`→`geometry.lua`, `keylogger/init.lua`→`watchers.lua` (suite `test:hs` 2317/0). Le **cœur** restant de `gestures.ahk` (catalogue `GESTURE_ACTIONS` + boucles d'extension + dispatch + hotkeys) est entrelacé et exécuté au top-level → limite des extractions propres atteinte.
- [x] Sortir l'UI de `lib/` → `ui/<window>/` : `onboarding`, `healthcheck`, `tooltip`, `changelog`, `hotstrings_config_window`, `model_browser`, `wpm`, `spotlight` ✅ (l'éditeur de hotstrings vit dans `ui/editors.ahk` + `ui/personal_toml_editor.ahk`). Plus aucun fichier-fenêtre dans `lib/` (seul `window_utils.ahk`, un helper, y reste).
- [x] Consolider la couche layout → `modules/keymap/` ✅ (commit `5d4fec87f`).
- [x] Renommages 1:1 : `menu_renderer.ahk`→`manifest_menu.ahk` ✅ (`6f0ddacd9`) ; `llm_model_browser.ahk`→`ui/model_browser/` ✅ (`e5cb83ef4`) ; `tooltip_llm.ahk`→`ui/tooltip/` ✅ (`6f0ddacd9`).
- [x] ~~Supprimer le stub `lib/app_state.ahk`~~ → **REJETÉ** (comme les items P3). À l'inspection, ce n'est plus un stub mort : c'est (1) la **doc canonique** des emplacements de l'état mutable + la règle « ne jamais réintroduire de Map d'état parallèle », et (2) **l'ancre d'un test de régression** (`tests/meta/test_appstate_orphaned_parallel_state.ahk` lit ce fichier précis pour garantir que la Map `AppState` crashogène ne revient jamais). Les accesseurs `AppState_*` vivent correctement dans leurs modules de feature (`nav_layer_helpers.ahk`, `layout.ahk`), pas ici. Le supprimer pour la parité-miroir cosmétique imposerait de réécrire un garde-fou + toucher 4 `#Include` → risque net négatif. **Conclusion : `lib/app_state.ahk` reste, comme doc + ancre de test.**
- [x] *(cosmétique)* extraire `lib/locale.ahk` d'`i18n.ahk` ✅ **déjà fait** (session antérieure) — `lib/locale.ahk` (10.9 Ko) existe, `#Include`é dans l'entrée + `run_all`, et contient la couche chargement-des-chaînes complète (cache TSV self-healing `_I18nLoadLocaleMap`/`_I18nEnsureActiveLoaded`/fallbacks EN-FR + l'accesseur `t(Key)` ligne 283). `i18n.ahk` ne garde que la GESTION de locale (détection système, sélection/persistance, menu de langue), exactement le miroir du split `macos/lib/{i18n,locale}.lua`. En-tête des 2 fichiers documente la frontière.

**Vérif.** Dry-run + suite + e2e verts ; menu/LLM/tooltip/onboarding/updater testés ; graphe d'include résout sans symbole manquant.

---

## P6 — Symétrie macOS

> **Réconcilié avec le réel le 2026-06-22** : plusieurs items étaient déjà faits, et un reposait sur une hypothèse fausse (`download_window` est vivant). Baseline Lua verte (`lua tests/run.lua` → all passed).

- [x] `lib/{mlx,ollama}_deps_checker.lua` → `modules/llm/` ✅ (déjà fait — `modules/llm/{mlx,ollama}_deps_checker.lua`).
- [x] `modules/hotstrings_config.lua` → `modules/hotstrings/` ✅ (déjà fait — requis partout comme `modules.hotstrings.hotstrings_config`).
- [x] `windows/README.md` + `macos/README.md` + `docs/TESTING.md` ✅ (déjà présents).
- [x] ~~Supprimer les assets morts `ui/download_window/*`~~ → **REJETÉ** : `ui/download_window` n'est PAS mort — il est `require`é activement par `mlx_deps_checker.lua` et `ollama_deps_checker.lua` (UI unifiée de progression de téléchargement LLM) et n'a pas de copie dans `_shared/`. C'est la seule copie, vivante. À garder.
- [x] **Splitter les 2 god-files MLX** : `modules/llm/api_mlx.lua` + `ui/menu/menu_llm/models_manager_mlx.lua` ✅ (les deux décomposés ; détail ci-dessous). *(Vérifiable par la suite unitaire Lua ; reload Hammerspoon par le mainteneur pour confirmer, comme les splits AHK P4/P5.)*
  - [x] **`models_manager_mlx.lua` : 1790 → 335 l.** ✅ — factory devenue un orchestrateur mince qui câble 3 modules frères via le mixin `install(ctx)` partagé : `models_manager_mlx_hf.lua` (287 l. — auth HuggingFace + source des modèles), `models_manager_mlx_server.lua` (643 l. — cycle de vie du serveur `start_server` : adoption inter-session, sweeps de port, launcher bash détaché, sondes de readiness, auto-récupération de crash), `models_manager_mlx_download.lua` (690 l. — `pull_model` + `reattach_download`). Le reste (`get_installed_models`, `check_requirements`, `delete_model`, hook de restart) vit dans l'index. Tests source-grep repointés (port, GC-pin, gc-retention) ; le test d'adoption nettoie aussi les modules frères pour que leur `local hs` de chargement recapte le `_G.hs` stubé. Suite verte (2367/0), lint + headers OK.
  - [x] `modules/llm/api_mlx.lua` : **1038 → ~700 l.** ✅ — décomposé en frères `api_mlx_inference.lua` (721 l. — mécanique requête/streaming) + `api_mlx_fetch.lua` (259 l. — stratégies batch/parallel/sequential) + **`api_mlx_discovery.lua` (NOUVEAU, ~340 l. — découverte des routes d'endpoint + cache routes/identifiants)**. La découverte était la seule sous-partie à coupure propre : c'est une feuille (ne dépend que des adapters + de l'URL de base injectée). Elle possède désormais les URLs `completions`/`chat` cachées, les identifiants de modèle (`server_model_id` / `expected_model_id` / `model_hf_path`), les flags + la file de callbacks de découverte, le client HTTP de sonde dédié, `read_active_model_arg`, et `discover()`/`reset()`. `api_mlx` garde la machine à états warmup/streaming/zombie-kill + la config de port ; il lit les routes via les getters de discovery (closures, donc set_port/discovery/cancel tardifs toujours vus), pousse la nouvelle URL de base sur `set_port`, et purge l'état de découverte via `reset()` dans `reset_endpoints`. Dépendance à sens unique (contrôleur → discovery), comme le pattern frère `inference`/`fetch`. **`warmup` reste dans `api_mlx`** : c'est l'orchestrateur (lit readiness + routes + déclenche la découverte) ; l'extraire forcerait un couplage bidirectionnel du cache de routes → laissé en place à dessein. Relocation pure, comportement inchangé. 3 tests source-grep repointés (sonde negative-status, fan-out callbacks `ipairs`, ordre give-up-après-découverte). Suite verte (2380/0), lint + headers OK. **⏳ En attente de test reload + MLX live par le mainteneur (cold load → ready, switch de modèle, changement de port, streaming).**
- [~] **Extraire les algos inline d'`init.lua`** (découverte hotstrings, cleanup MLX, scan personal-hotstrings, file-watcher) → modules/lib ; renuméroter les bannières ; renommer le `boot_llm_enabled` dupliqué. ⚠️ Sensible à l'ordre de boot (comme l'entrée Windows P4) — incréments vérifiés.
  - [x] **Renommer le `boot_llm_enabled` dupliqué** ✅ — la passerelle rapide précoce devient `mlx_cleanup_enabled` (deux `local boot_llm_enabled` se masquaient mutuellement ; noms distincts = intention non ambiguë).
  - [x] **Cleanup MLX → `modules/llm/boot_cleanup.lua`** ✅ — le bloc synchrone (épargne-un-serveur-sain / nuke-le-reste, lsof+curl+pgrep) déplacé verbatim dans `run_selective_cleanup()` ; `init.lua` garde la passerelle et appelle le module. Smoke-test du contrat ; 2 meta-tests source-grep repointés (`test_init_mlx_cleanup_gated`, `test_audit_v5_fixes`).
  - [x] **`safe_dir_entries` → `lib/fs_dir.lua`** ✅ — le wrapper béni de `hs.fs.dir` (throw- et state-safe, gardien de *fsdir-drops-state*) était dupliqué dans `init.lua` et `ui/hotstrings_config_window/init.lua`. Extrait dans `lib/fs_dir.entries` ; les deux sites l'aliasent (`local safe_dir_entries = require("lib.fs_dir").entries`) → tous les appels inchangés, contrat en un seul endroit. La copie de la fenêtre (silencieuse) journalise maintenant comme la version canonique. Meta-test `test_fs_dir_iterator_contract` section 3 repointée ; sections 2 (scan source) + 4 (fidélité stub) couvrent déjà le module ; smoke-test runtime ajouté.
  - [ ] **`has_common_hotstring_groups` + `HOTSTRINGS_EXCLUDED_STEMS`** — laissés dans `init.lua` : usage unique (sélection du dossier de hotstrings au boot), non dupliqués → les sortir n'apporterait que de l'amincissement, au prix d'un risque boot. Lecture linéaire au point d'usage préférée.
  - [ ] **scan personal-hotstrings + file-watchers** — **non faits, recommandés à laisser** : flux de données entrelacé (ordre de chargement = priorité des groupes) et GC-rooting des `hs.pathwatcher`, avec **modes d'échec silencieux** (auto-reload cassé, priorité faussée) qu'un test rapide ne révèle pas et qu'aucun test unitaire ne couvre (la suite ne charge pas `init.lua`). Rapport risque/gain défavorable ; à ne tenter qu'en session dédiée reload-vérifiable si vraiment souhaité.
- [x] `new-driver.js` : `DRIVER_SUBDIRS` ✅ — déjà canonique (`['adapters','lib','modules','ui','data','_generated','tests']`).
- [x] ~~Déplacer `data/generate_models.py` + `pyproject.toml` + `uv.lock` → `tools/`~~ → **REJETÉ (décision mainteneur, 2026-06-23)** : `pyproject`/`uv.lock` sont la racine de l'env `uv` du driver macOS (le `.venv` que résout le code MLX + le `uv sync` de CONTRIBUTING). Move non sec, risque de casser la résolution du venv MLX pour un gain purement cosmétique. Laissés en place.
- [x] `ui/paths_editor/` & `ui/token_prompt/` ✅ — **pas d'`init.lua` nécessaire** : dossiers d'assets webview uniquement (`index.html`/`script.js`/`style.css`), chargés via `assets_dir`, jamais `require`és comme modules Lua. No-op confirmé.

**Vérif.** Suite macOS verte (`lua tests/run.lua`) ; ordre des phases de boot (log) inchangé — reload Hammerspoon par le mainteneur.

---

## P7 — Mise en commun profonde (gros blast radius — en dernier, livré sous-étape par sous-étape)

- [x] **Loggers HS + AHK rendus exactement équivalents** ✅ (directive mainteneur : « rends-les exactement pareil, aligne sur la meilleure version »). Le cœur partagé `_shared/lua/logger` (+ `set_sink`) existe déjà et sert le daemon Linux ; les deux loggers de PROD (HS `macos/lib/logger.lua`, AHK `windows/lib/logger.ahk`) sont maintenant alignés sur le contrat canonique :
  - **Format identique** : `YYYY-MM-DD HH:MM:SS:mmm [LEVEL] [module] body`. Suppression de l'indentation de 10 espaces des lignes DEBUG côté macOS (déviation propre à HS ; le SPEC, le cœur partagé, AHK et Linux étaient déjà sans indent).
  - **Dedup identique** : suppression des lignes consécutives identiques (TOUS niveaux) dans une fenêtre de 5000 ms, puis un résumé `[LEVEL] [logger] ↑ N identical lines suppressed`. Union du meilleur des deux : macOS gagne la fenêtre (corrige sa suppression-permanente latente, le bug que le garde AHK `logger-dedup-tick` interdit) ; AHK passe d'ERROR-only à tous-niveaux + résumé. État dedup AHK passé en globals (réinitialisable par les tests). Clé de dedup = identité sans horodatage des deux côtés.
  - **Marqueur de session** : AHK écrit désormais la même bannière `===== <ts> — ErgoptiPlus session opened =====` que HS (une fois/session, log principal). `—` et `↑` via `Chr()` pour rester ASCII-safe.
  - Différences restantes = intégration platform-specific non observable (numérotation interne des niveaux 1-4 vs 10-40 — sortie identique car basée sur les labels ; source de config hs.settings vs config.toml ; capture print() macOS ; hooks healthcheck AHK). +1 test de régression AHK (collapse + résumé). Suite AHK **2447/0**, HS **2380/0**, encoding 622, lint vert. **⏳ En attente de reload-test (macOS + Windows) par le mainteneur.**
- [x] **Tooltip AHK lit `_shared/modules/tooltip/constants.toml`** ✅ (déjà fait — case périmée). `lib/ui_style.ahk` (`UiStyle_LoadSharedConst`) lit le TOML partagé au boot, fail-fast, et `macos/ui/tooltip/config.lua` fait de même au require-time. Les divergences d'alpha sont résolues par les clés per-platform (`border_alpha_ahk`/`_hs`, `sep_alpha_ahk`/`_hs`, `window_bottom_inset_ahk`/`_hs`, `canvas_alpha_hs`). Aucune constante tooltip n'est inlinée hors du loader. Rien à faire.
- [~] **tap_hold → `_shared/tap_hold/defaults.toml`** — **décision mainteneur 2026-06-24 : faire le FEAT d'unification** (les 2 défauts sont des paradigmes différents, voir preuve ci-dessous ; unifier change le comportement par défaut → assumé). **Incrément 1 fait** ✅ (`cfe4b555a`) : relocalisation `windows/data/tap_hold/defaults.toml` → `_shared/tap_hold/defaults.toml` + repointage des 2 consommateurs AHK (`ErgoptiPlus.ahk` boot loader, `first_boot.ahk` seed) sur `_SharedDir` + toutes les refs doc/schéma. Relocation pure, zéro changement AHK ; le loader AHK documentait déjà cette source. Suite AHK 2463/0, validate 0, encoding 625, lint vert. **⏳ reload-test Windows (first-boot seed depuis le nouveau chemin).** **Reste** : (inc 2) restructurer le fichier partagé en superset (section AHK = actuel ; ajouter section HS reproduisant `macos/modules/karabiner/defaults.lua` → préserve le comportement) ; (inc 3) porter le générateur Karabiner macOS pour lire la section HS du TOML au lieu de `defaults.lua` ; (inc 4) replier `tap_hold_keys.json`/`mod_combos.json` dans le TOML partagé si pertinent. **Preuve de divergence** (pourquoi c'est un feat, pas un refactor) : AHK = 7 touches PC, `time_activation_seconds` par touche, `tap_action`+`hold_modifier`/`hold_layer`, système de *layers* nav/nav_secondary (HJKL→flèches) ; macOS = 14 touches Mac (fn/cmd/option…), timeout global `tap_hold_timeout_ms=1000`, matrice N×N de *combos* (143 chords simultanés) sans équivalent AHK, vocabulaire d'actions distinct (`actions.json` ~150) + `layer_keys.json`/`capsword.json` ; défauts d'actions différents sur touches communes (caps_lock Win enter/ctrl vs Mac return/cmd ; left-ctrl Win paste vs Mac cut ; tab Win alt_tab_monitor/alt vs Mac alt_tab_windows/fn). Shape d'unification retenue par défaut : **superset à sections per-platform préservant le comportement** (pas de défauts canoniques forcés, sauf demande explicite touche-par-touche du mainteneur). **Incrément 2 fait** ✅ (`743205352`) : les défauts macOS (timeouts + 14 touches tap/hold + matrice 14×13 = 182 combos) sortis de la table Lua hand-maintained `modules/karabiner/defaults.lua` vers les sections `[hs_timeouts]`/`[hs_tap_hold]`/`[hs_combos]` du fichier partagé ; `defaults.lua` devient un loader mince qui parse ces sections via `lib.toml_reader` au require-time et reconstruit l'API positionnelle exacte (`D.tap_hold[k]={tap,hold}`, `D.combos[id]={combo,tap,hold}`), fail-fast sur fichier/section manquant. Bloc `[hs_*]` généré depuis l'ancien `defaults.lua` et **vérifié byte-à-byte** (chaque valeur, 0 entrée manquante/en trop). AHK ignore `[hs_*]` (parser étroit) ; macOS ignore les sections `[tap_hold.*]` pointées. +4 tests de régression (compte 14/182, valeurs sentinelles, asymétrie `rcmd_lcmd` option_shift) → un parse partiel ne peut plus rétrécir silencieusement les défauts. Suite Lua macOS **2384/0**, suite AHK **2463/0** (loader tap-hold vert), lint vert, node 14/14. **⏳ reload-test macOS (régénération Karabiner + « réinitialiser aux défauts »).** **Cœur FEAT A atteint** : les *défauts* tap-hold sont unifiés dans un fichier partagé que les deux drivers lisent ; la duplication (data `defaults.lua`) est éliminée. **Inc 4 écarté (net négatif)** : `tap_hold_keys.json` (définitions de touches : key_code Karabiner + libellés UI) et `mod_combos.json` (définitions de combos : key_codes simultanés + libellés/groupes) sont des **définitions structurelles macOS-Karabiner pures** (comme `layer_keys.json`/`capsword.json`/`actions.json`), pas des défauts cross-driver — les plier dans `_shared/tap_hold/defaults.toml` ne ferait que reloger de la donnée macOS-only qu'AHK ignore, pour zéro valeur cross-driver. Elles restent légitimement dans le driver macOS.
- [~] Codegen du codec TOML + parser LLM AHK depuis `_shared/lua` ; éliminer `path_translator.ahk`.
  - [x] **TOML codec — parité déjà certifiée** ✅ : corpus fuzz cross-driver partagé `_shared/tests/corpus/toml/fuzz_corpus.json` (50 vecteurs) exécuté par les DEUX drivers (`test_corpus_toml_fuzz.ahk` + `test_toml_fuzz.lua`) — crash-safety + classification ok/error identiques. **Transpile Lua→AHK rejeté** (mainteneur consulté) : le codec AHK (~1500 l.) est écrit-main et fonctionne ; le transpiler ferait courir un risque élevé sur un parseur qui marche, pour un gain nul (les langages ne peuvent de toute façon pas partager le code ; le corpus partagé EST la certification). Parité output byte-identique non poursuivie : ferait remonter des divergences de représentation entre 2 parseurs distincts → réconciliation risquée d'un code qui marche.
  - [x] **`path_translator.ahk` — SUPPRIMÉ ✅ (décision mainteneur : « supprime toute traduction, migre tout en v2 »).** Le menu lit/écrit l'état des features exclusivement via le cœur v2-natif `lib/feature_io.ahk`. **Incréments 10-11 faits** : les 2 tables encore référencées (`_LegacyTopCategoryMap`, `_LegacyDynamicHotstringsKeyMap`) déplacées dans `tray_menu.ahk` (données de menu, pas de traduction) ; `_V1CatToInverseKeyMap` mort (jamais lu) retiré ; `lib/path_translator.ahk` entier supprimé (−817 l. : `TranslateLegacyPath`, `LegacyPathToManifestPath`, `ManifestPathToLegacyPath`, `WriteFeatureBatch`/`Update`, `GetFeatureState`, toutes les tables de renommage) + son `#Include` + les stubs TapHold morts (`IsTapHoldVariantActive`, `TapHoldVariantLabel`, `TapHoldGroupLabel`). `/validate` vert sur l'entrée prod ; suite AHK 2463/0. *(Reste hors-scope de ce chantier : le gating de catégories utilise encore des ids PascalCase `CategoryEnabled["Hotstrings"]` — c'est la couche menu, pas de la traduction de chemins ; migration séparée si souhaitée.)* Détail des incréments ci-dessous. Stratégie : un cœur v2-natif `lib/feature_io.ahk` (`FeatureLocateV2` + `WriteFeatureV2`/`WriteFeatureBatchV2`/`ReadFeatureStateV2`) qui résout un chemin manifest v2 → `{section, key, v2_node, is_alpha}` par INTROSPECTION de `Features` (Map-avec-`enabled` = α ; feuille bool = plain), sans aucune table de renommage PascalCase. Le chemin manifest porte déjà la section complète (préfixe `ahk.` inclus, que `Features` strippe). **Incrément 2 fait** ✅ : `feature_io.ahk` + 8 tests de localisation (le translator n'en avait aucun) + toggle Gestures routé via `WriteFeatureV2`. Suite AHK 2457/0. **Incrément 3 fait** ✅ : `ToggleFeatureV2` (remplaçant v2-natif de `ToggleMenuVariableByPath`) + `_HS_TryLiveToggleV2` (live-toggle hotstrings v2) + `_MutexSiblingPathsForV2` (siblings énumérés depuis la section manifest, plus aucune table de siblings écrite-main). `MenuAddItemFromManifest` — le builder de TOUT item manifest-driven (Layout, toutes catégories Hotstrings, Shortcuts plain/α + les sous-Maps modifier-combo mutuellement exclusives) — dispatche désormais via `ToggleFeatureV2` et lit son état (coche) via `ReadFeatureStateV2`. +3 tests manifest-backed (siblings mutex, strip préfixe `ahk.`, vide pour non-mutex). Suite AHK 2460/0. **Incrément 4 fait** ✅ : rendu manifest 100 % v2 — `MenuAddItemFromManifest` ne dérive plus de chemin v1 (garde de skip via `FeatureLocateV2`, libellés via le chemin v2) ; `_ApplyMenuLabelDynamicSubstitutions` entièrement v2-natif (`{date}` sur `hotstrings.dynamic.date[_fr|_long_fr]`, suffixe de compte via `StrReplace(cat,"_","")` → catégorie loader TOML, libellés byte-identiques) ; `GetMenuTitleByPath` lit le suffixe de lettre via `ReadFeatureStateV2` au lieu de `GetFeatureState`. Suite AHK 2460/0, `/validate` vert sur l'entrée prod. **Incrément 5 fait** ✅ : letter-pickers migrés — `MenuAddLetterPicker` lit `enabled`/`letter` via `ReadFeatureStateV2` et passe le chemin v2 alpha aux writers ; `SetFeatureLetter` persiste enabled+letter via `WriteFeatureBatchV2` (lettre via prop `letter`), `SetFeatureLetterOff` via `WriteFeatureV2`. Plus de `WriteFeatureBatch`/`WriteFeatureUpdate`/`GetFeatureState` v1 dans le chemin letter-picker (reste le `LegacyPathToManifestPath` résiduel pour `GetMenuTitleByPath`/greying). Suite AHK 2460/0. **Incrément 6 fait** ✅ : builders Personal migrés + **dispatcher v1 supprimé**. `MenuAddItemWithLabel` prend un chemin v2 et passe par `ToggleFeatureV2`/`ReadFeatureStateV2` ; les hotstrings personnels passent `hotstrings.personal.<lower(section)>`, les shortcuts personnels `ahk.shortcuts.personal.<name>` (label lu directement depuis `_PersonalShortcutsRegistry`). Les deux sous-arbres Features sont seedés au boot (`RegisterPersonalFeature`/bootstrap personal) → `FeatureLocateV2` résout sans le lazy-seed du translator. Cluster v1 mort supprimé : `ToggleMenuVariableByPath`, `_ResolveMenuItemEnabled`, `MenuAddItem`, `_HS_TryLiveToggle`, `_HS_ApplyLiveToggle`, `_MutexSiblingPathsFor` (les branches TapHolds y étaient déjà inatteignables — le menu tap-hold a ses propres writers `WriteTapHold*`). `−182/+26` lignes. `/validate` vert ; suite AHK 2460/0. **Incrément 7 fait** ✅ : batch « tout activer » hotstrings migré — `_CollectAllHotstringsV2Paths` (chemins manifest pour flat+dynamic, `hotstrings.personal.<lower(section)>` pour le personnel) + `WriteFeatureBatchV2` dans les deux sites `config_io`. Les ids dynamiques viennent du manifest (drop `_LegacyDynamicHotstringsKeyMap` ici) ; `_CollectAllHotstringsV1Paths` (dernier appelant `ManifestPathToLegacyPath` du menu) remplacé. **`WriteFeatureBatch`/`WriteFeatureUpdate`/`GetFeatureState` n'ont plus AUCUN appelant hors `path_translator.ahk`** (morts, supprimés à la fin). `/validate` vert ; suite AHK 2460/0. **Incrément 8 fait** ✅ : `manifest_descriptions` ne calcule plus `ManifestPathToLegacyPath` — le candidat locale folded-v1 qu'il alimentait est identique au folded-v2 déjà généré sauf pour DynamicHotstrings, et aucune locale ne clé en `dynamichotstrings.*` ; param `V1Path` retiré des 3 fonctions internes. **Incrément 9 fait** ✅ : letter-pickers 100 % v2 — `MenuAddLetterPicker(MenuParent, V2Path, MasterCategory)` + `GetMenuTitleByPath(V2Path)` (branches Personal/TapHolds mortes supprimées) ; `_MR_BuildBuiltinGroup`/`_MR_RenderLetterPicker` passent des chemins v2 (`shortcuts.e_grave`…), ids manifest `accented_letters_group` en snake_case. **`LegacyPathToManifestPath` ET `ManifestPathToLegacyPath` n'ont plus AUCUN appelant hors `path_translator.ahk` ; toutes les fonctions publiques du translator sont mortes.** Suite AHK 2465/0. **Reste (inc 10-11)** : relocaliser les TABLES de données encore lues par les builders (`_LegacyTopCategoryMap` dans `_CollectAllHotstringsV2Paths`/`_CountEnabledForCategory`, `_LegacyDynamicHotstringsKeyMap` + `_DYNAMIC_HOTSTRINGS_ORDER` dans `_BuildDynamicHotstringsSubmenu`) hors de `path_translator.ahk` ; gating de catégories (PascalCase `IsCategoryGated`/`_MasterCategoryFor`→v2) ; stub `IsTapHoldVariantActive` mort ; puis **supprimer `path_translator.ahk`**. *Ancienne décision « garder le pont » écartée par le mainteneur.*
  - [s] *(note historique)* L'analyse initiale recommandait de garder le pont : `path_translator` n'est PAS un simple mappeur PascalCase→snake_case : c'est le **pont de compatibilité v1→v2** complet du menu (cf. `_migration_v1_to_v2.md`, 66 Ko). **Incrément 1 fait** ✅ : le toggle Gestures (seul site vraiment isolé + hardcodé) migré en écriture v2-native (`Features["gestures"]["enabled"]` + `TOML_Write(... "ahk.gestures" "enabled")`) — vérifié que la clé Features (`gestures`) ≠ section TOML (`ahk.gestures`). **Reste arrêté** : les ~19 refs restantes sont la couche d'interaction menu intégralement v1 (`ToggleMenuVariableByPath`, `GetFeatureState`, `GetMenuTitleByPath`, letter-pickers, gating de catégories en PascalCase `Hotstrings`/`Shortcuts`/…) + les fichiers de locale partagés clés-en-v1 + le mapping hotstrings α/dynamic/personal. Ce n'est pas une suite de call-sites mais **une réécriture indivisible de toute la couche menu** (jours, risque très élevé — casse tous les toggles si erreur — pour **zéro gain** utilisateur/cross-driver). Le pont est délibéré, testé, aligné-manifest (comme `lib/app_state.ahk`, gardé). **Décision : garder le pont.** Même catégorie net-négatif que `tap_hold` et la réconciliation du parseur TOML, écartés à juste titre.
- [~] Promouvoir les frontends web restants → `_shared/ui/` ; plier le placement du menu dans `manifest.toml` + émettre l'arbre par codegen. **(FEAT B + FEAT C — décision mainteneur 2026-06-24.)**
  - **FEAT B (menu → manifest + codegen)** — **inc 1 fait** ✅ (`6983665bf`) : drift gate `tools/test/test-menu-manifest.cjs` (câblé dans `test:js`, 15 checks) qui verrouille `menu_manifest.json` contre ses sources — chaque item `feature` résout vers une feature OU une section de `manifest.toml` (les sections combo-modificateur s'étendent en sous-menus mutex), chaque clé i18n existe en fr+en, `platforms` ∈ {ahk,hs}, `toggle` a une `category`. C'est la couche de validation que tout arbre de menu généré devra passer, et elle attrape le vrai risque (un `path`/clé i18n périmé rendait un toggle mort silencieusement). **Constat de l'investigation** : `menu_manifest.json` (649 l.) est DÉJÀ une source unique partagée lue par les 2 drivers au runtime — ce n'est pas dupliqué. Le « plier dans manifest.toml + émettre par codegen » = transcrire 650 l. de données de présentation hétérogènes (8 types d'items, gesture_slots, word_delimiter_defs avec chars tab/`\`/`'`) dans le manifest.toml de 2650 l. + un émetteur qui reproduit le JSON imbriqué exact. Coût élevé/fragile, mêle 2 préoccupations (défs de features vs présentation menu), pour « un fichier de moins ». **Inc 2 fait** ✅ (`6e0e1a023`) : structure menu repliée dans `manifest.toml [menu.*]` (générée depuis le JSON actuel pour fidélité) ; nouveau codegen `tools/build/build-menu-manifest.js` (`npm run build:menu`) émet `_shared/modules/menu/menu_manifest.json` ; câblé dans `build:domain` + drift-gated comme les autres artefacts générés. **`menu_manifest.json` régénéré vérifié sémantiquement deep-equal au fichier d'origine** (données menu identiques ; seuls l'en-tête `_comment` + l'ordre des clés/format diffèrent) → comportement préservé sur les 2 plateformes, drivers inchangés (lisent le JSON généré au runtime). Le menu a désormais une SSoT unique dans `manifest.toml`. build:domain 11/11 (incl. build:menu + drift-check), test:js 15/15, suite AHK 2463/0, Lua macOS 2384/0. **⏳ reload-test (tray menu identique sur Windows + macOS).** **FEAT B terminé.**
  - **FEAT C (frontends web Windows → webview `_shared/ui`)** — **scopé, pas encore implémenté.** Investigation faite. **Le pont bidirectionnel WebView2 EXISTE déjà** côté Windows : `ui/model_browser/init.ahk` boote WebView2 (`WebView_SharedEnvironment` + gate RAM + fallback ListView natif), reçoit JS→AHK via `WebMessageReceived`, injecte AHK→JS, et consomme le frontend partagé `_shared/ui/model_browser/`. Donc convertir une fenêtre = **cloner ce pattern**, pas construire un pont. Infra : `lib/webview_utils.ahk` (`WebView_SharedEnvironment`/`WebView_ShouldUseNativeFallback`) + `vendor/WebView2.ahk`. **Frontends déjà partagés** (`_shared/ui/`, JS host-agnostique webkit↔chrome.webview) : model_browser, download_window, changelog, metrics_typing, metrics_apps. **Frontends macOS-only à promouvoir** (`macos/ui/` → `_shared/ui/`) : onboarding, hotstring_editor, hotstrings_config_window, personal_info_editor, prompt_editor, paths_editor, token_prompt. **Fenêtres natives Windows cibles** : `ui/onboarding/` (1612 l.), `ui/personal_toml_editor.ahk` (1093 l. — éditeur hotstrings perso), `ui/editors.ahk` (114 l. — 4 modals simples dont infos perso + lien GPT), `ui/action_picker.ahk` (184 l. — mais PAS de twin partagé, créerait un frontend neuf). **Plan window-by-window** (chaque fenêtre = un incrément reload-testé sur LES DEUX plateformes, car ça change l'UX Windows et touche macOS) : (1) promouvoir le frontend macOS → `_shared/ui/<window>/` en rendant son `script.js` host-agnostique ; (2) repointer macOS sur `_shared/ui` + vérifier macOS ; (3) host WebView2 Windows cloné de model_browser + pont 2-way ; (4) remplacer le Gui natif (fallback natif conservé) ; (5) reload-test 2 plateformes. **Constat de candidature** (vérifié) : pas de fenêtre « simple ». `token_prompt` + `hotstrings_config_window` = macOS-only (pas de twin Windows) ; `paths_editor` = macOS-spécifique ; `personal_info_editor` = pont HTTP-server macOS (incompatible WebView2 postMessage, nécessiterait une migration de pont). Seuls candidats pont-postMessage + twin Windows : **onboarding** (~1612 l.) et **éditeur hotstrings** (`personal_toml_editor.ahk`, ~1093 l.). **Mainteneur a choisi onboarding en premier.**

  - **FEAT C — onboarding, inc 1 fait** ✅ (`21ca0e7ad`) : frontend onboarding (index.html/script.js/style.css) déplacé `macos/ui/onboarding/` → `_shared/ui/onboarding/` ; host macOS `init.lua` lit via `Paths.shared("ui/onboarding")` (chemin image préservé, même profondeur sous `static/`). Relocation pure, octets identiques, comportement macOS inchangé. luac OK, Lua 2384/0, test:js 15/15 (bundle layout + file-headers). **Constat de viabilité (inc 2)** : le frontend partagé présente le MÊME flux 6 étapes avec les MÊMES choix que l'onboarding natif Windows et collecte exactement les 6 mêmes réponses (`locale, config_dir, use_ergopti, magic_key, use_metrics, use_gestures`). La divergence est UNIQUEMENT dans le handler `finish` du host (quelles clés config écrire) — code host per-platform, pas le frontend. Donc le host WebView2 Windows route `finish` vers le **`_Onboarding_Commit` EXISTANT** (`finish.ahk`, qui écrit déjà les bonnes clés Windows : `ahk.layout.{ergopti_base,ergopti_alt_gr,ergopti_plus}`, `ahk.metrics.metrics_enabled`, `script.locale`, `ahk.gestures.enabled`) — réutilisé, pas réécrit. Le hic Windows-spécifique (boutons PowerShell auto-register gestes de l'étape 5) est géré par le flag différé **déjà existant** `ahk.gestures.auto_configure_on_next_start` (set si `use_gestures=true` → `gestures.ahk` lance le PowerShell au prochain boot). **Plan inc 2** : host WebView2 cloné de `ui/model_browser/init.ahk` (env partagé + gate RAM + fallback Gui natif conservé) ; handlers des 6 messages (`ready`→injecter initData {locales, strings, system_layout, defaults, image layout} ; `previewLocale`→applyStrings ; `localeSelected` ; `pickConfigDir`→DirSelect→setConfigDir ; `loadExistingConfig`→lire config.toml→applyExistingAnswers ; `finish`→mapper 6 réponses vers `_Onboarding_Commit` + flag gestes différé). ⚠️ ~400 l. de host, **critique premier lancement, vérifiable SEULEMENT au reload Windows** (la suite AHK confirme le parse, pas le rendu/pont webview). **Inc 2 fait** ✅ (`089790e96`) : host `ui/onboarding/webview.ahk` cloné de model_browser (env partagé + gate RAM + fallback Gui natif), 6 handlers de pont, `finish` → mêmes globals `_ob_*` + `_Onboarding_Commit` existant (config écrite identique au natif), sentinelle `_ob_gui` préservée pour le park-loop. Frontend `script.js` rendu host-agnostique (`_post` + ready : branche webkit macOS d'abord, sinon `chrome.webview` Windows) → un seul frontend pour les 2 drivers, macOS inchangé. `Onboarding_Run`/`ShowFromMenu` essaient le webview puis retombent sur `_Onboarding_Step1`. entry+run_all /validate 0, suite AHK 2465/0, encoding 626, lint vert, test:js 15/15. **⏳ reload-test premier lancement Windows** (supprimer la config user ou « relancer l'assistant » depuis le tray) — c'est aussi la validation du PATTERN de pont WebView2 que les fenêtres suivantes (éditeur hotstrings…) réutiliseront. **Fenêtre suivante** : éditeur hotstrings (`personal_toml_editor.ahk` → frontend `hotstring_editor` partagé, pont CRUD), après confirmation reload de l'onboarding.

  - **FEAT C — fenêtres suivantes livrées** (le pattern de pont WebView2 validé par l'onboarding est désormais réutilisé ; le constat de candidature de l'inc précédent est dépassé — un host Windows est viable même sans twin natif en réutilisant la couche données existante, et `personal_info_editor` a été migré du pont HTTP-server vers postMessage) :
    - **éditeur hotstrings perso, fait** ✅ (`a17247798`) : `personal_toml_editor.ahk` → frontend `_shared/ui/hotstring_editor/` via host WebView2, pont CRUD ; + correctifs style (dark-mode, pilules, étoile déclencheur, libellés `{truc}` capitalisés toutes locales), champ de recherche trigger/output, fix suppression (wiring confirm-dialog), skip `_meta` dans `LoadExtTomlFile`.
    - **paths_editor, fait** ✅ (`f60ba8f62`) : frontend → `_shared/ui/paths_editor/` (`script.js` host-agnostique, handler `hsPaths`) ; host WebView2 Windows `ui/paths_editor/init.ahk` (vhost `ergopti.paths`, save→`paths.toml`+Reload) branché en tête de `FilePathsEditor` ; macOS repointé via `Paths.shared`.
    - **personal_info_editor, fait** ✅ (`dbc95b870`) : frontend → `_shared/ui/personal_info_editor/` (app standalone, plus de page navigateur) ; macOS migré du **serveur HTTP** vers WKWebView postMessage ; host WebView2 Windows `ui/personal_info_editor/init.ahk` (champs + indices magic depuis la config live, save→TOML perso+Reload) ; pas de scroll horizontal ; test port-leak réécrit (assert no-HTTP-server).
    - **hotstrings_config_window, fait** ✅ (`2bdae60aa`) : frontend → `_shared/ui/hotstrings_config_window/` (`send()` host-agnostique) ; host WebView2 Windows `ui/hotstrings_config_window/webview.ahk` qui **réutilise la couche données du Gui natif** (`_HCW_BuildCategoryList`/`_HCW_Resolve`/`_HCW_TomlDefaults`/`_HCW_UserOverride`/`_HCW_SetOverride`/`_HCW_ClearOverride`/`_HCW_PatchTomlMeta`) et n'échange que la présentation ; `OpenHotstringsConfigWindow` essaie le webview puis retombe sur le Gui natif (tous les tests meta natifs restent verts) ; parité d'état/actions gardée par un test de contrat dédié.
    - **prompt_editor, fait** ✅ (`c1394ab98`) : frontend → `_shared/ui/prompt_editor/` (`post()` host-agnostique, éditeur chips + autocomplete) ; host WebView2 Windows `ui/prompt_editor/init.ahk` (create/edit by id, mappe `prompt`↔`system_single`, persiste via `LLM_Menu_SaveConfig`+`LLM_Engine_Init`+`LLM_Menu_Build`+rebind hotkeys) branché en tête de `LLM_Menu_PromptCreateProfile`/`PromptEditProfile` ; **assistant InputBox natif conservé en fallback**.
    - **token_prompt, frontend partagé seulement** ✅ (`569a82be3`) : déplacé vers `_shared/ui/token_prompt/` + `post()` host-agnostique, macOS repointé via `Paths.shared` ; **pas câblé sur Windows** — l'UI n'est atteignable que depuis le gestionnaire de modèles MLX/HuggingFace, **absent sur Windows (Ollama uniquement)** ; décision mainteneur 2026-06-25 : partager le frontend pour préparer un futur host Windows, sans construire le backend MLX. *(Construire le backend MLX/HuggingFace Windows = nouvelle feature, hors-scope du partage d'UI.)*
    - **download_window** : déjà partagé/porté — frontend `_shared/ui/download_window/`, host Windows `modules/llm/ollama_webview.ahk` (le « macOS-only » d'une analyse antérieure était faux). Aucune action.
    - Gates à chaque incrément : `/validate` 0, suite AHK verte (2474→2476/0), encoding (632 fichiers BOM+CRLF), file-headers + lint stricts, test:js (20 checks), Lua macOS 2384/0. **⏳ reload-tests Windows** (rendu/pont webview non couvert par la suite) pour chaque nouvelle fenêtre.

**Vérif.** Corpus `_shared/tests/` identique sur les deux moteurs ; test de conformité logger exerce la prod ; parité constantes tooltip ; suite + drift gate verts après chaque sous-étape (livrée indépendamment).

---

## Décisions maintainer (à trancher avant les phases concernées)

1. **layout/keymap Windows** : `lib/keymap/` ou `modules/keymap/` ? (macOS = `modules/keymap/`.)
2. **Placement menu** : plié dans `manifest.toml` (1 SSoT, gros codegen) ou `menu_manifest.json` + drift gate ?
3. **`lib/` Windows foldéré vs `lib/` macOS plat** : converger dans quel sens ?
4. **`_generated/registry.ahk` + `expander.ahk` orphelins** : ✅ RÉSOLU (audit 2026-06-26, GEN-1/2) — **supprimés**. Orphelins prouvés par grep (0 `#Include`, 0 instanciation) ; les tests `test_domain_{registry,expander}.ahk` exercent le moteur vivant `HSE_*` (`hotstring_engine_main.ahk`), pas les classes générées. Scripts codegen + npm + steps drift-gate retirés ; garde `SubStr(-0)` repointé sur le moteur vivant.
5. **Codec TOML + parser LLM AHK** : transpile Lua→AHK ou génération corpus-driven + bannière hand-port ?
6. **`linux/`** : même passe ou suivi séparé ? (Au min. vérifier l'absence de surface de crash `Features` brute.)

---

## Risques transverses

- **AHK sensible encodage/espaces** : tout split risque l'abort silencieux mi-fichier si LF-only/sans BOM. → Edit tool (jamais `cat >>`), tests ASCII-only via `Chr(0xNNNN)`, lancer `test:ahk-encoding` après chaque move.
- **Ordre d'`#Include` + `#InputLevel`** portent des invariants réels (hoisting globals, `#InputLevel 2` avant les includes de layer). Réordonner casse les hotkeys **sans erreur de compile** → préserver l'ordre, vérifier la séquence de boot.
- **`run_all.ahk` auto-découverte (P2)** : changer l'ordre de chargement peut révéler des dépendances inter-tests latentes → stager derrière le test d'intégrité.
- **`tap_hold` partagé (P7)** : probablement déjà divergent → traiter comme `feat` si non prouvé identique.
- **Churn de fichiers générés** : le drift gate produit un gros diff one-time → commit dédié.

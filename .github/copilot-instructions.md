# GitHub Copilot adapter

`AGENTS.md` is the common repository contract. For detailed work, load only the
matching skill from `.agents/skills/` and the topics routed by
`docs/memory/README.md`; do not duplicate either catalog in prompt context.

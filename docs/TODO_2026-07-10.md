# TODO — ergopti — reste à faire (2026-07-10)

> La quasi-totalité des bugs daemon, la mutualisation SSoT et les tests de non-régression
> ont été livrés (voir l'historique git). **Le document de travail détaillé et à jour est
> [`docs/IMPROVEMENT_PLAN_2026-07-10.md`](IMPROVEMENT_PLAN_2026-07-10.md)** — audit multi-agents,
> revue post-exécution de l'agent (§1bis), checklist ordonnée (§6). Ce fichier-ci ne garde que
> le **résumé de ce qui reste**.

## Fait — ne pas rouvrir

- Horloge daemon (`os.clock` → luv monotone : gestes, `file_watchers`, timestamp keylogger).
- Dérivation runtime du slot-space gestes Linux ; retrait du fallback `kanata one_shot_ms` (fail-fast).
- Réconciliation des timings keylogger + drift gate ; sweep des refs de plan + gate zéro-hit + hook `commit-msg`.
- Lot 🟢 de l'agent exécutant (27 commits) **+ relecture Opus + correctifs** : suites remises au vert,
  régressions cassantes corrigées (`#60` compile AHK, `#75` privacy, `#76` crash dispatcher), tests
  tautologiques/orphelins réparés, fuites d'ID retirées. Détail complet en **§1bis** du plan.

## Reste à faire

### 🔴 Le plus important — le bug de frappe « abcd→acd »

- [ ] **`#66/#67` : implémenter le grab clavier (EVIOCGRAB) + ré-injection de toutes les frappes**
      sur Linux. L'agent n'a livré qu'un queue interne **inerte** (code mort en mode observe — voir
      plan §1bis). Décision §5.1 = grab par défaut ; validation hardware requise (§4.3). **C'est le
      vrai correctif de ton bug ; il n'est pas encore fait.**

### 🔵 Réservé Opus / mainteneur (décidé en session précédente)

- [ ] **i18n** : router + traduire les ~20 textes en dur dans les 21 locales (plan §3.4).
- [ ] **Structure** : `shell_runner` Linux + renommages `manager→init` + relocalisation sous `lib/` (plan §3.7).

### 🟠 Tests §5.9 manquants — fix corrects, filet de sécurité absent (plan §1bis)

- [ ] Ajouter le test rouge-avant prescrit pour : `#13`, `#18`, `#14`, `#15`, `#17`, `#12`, `#72`.
- [ ] `#6` : refaire le gate de normalisation couleur WPM (aujourd'hui **tautologique** — compare
      sa propre impl JS à des golden hardcodés) pour lire les vraies impls AHK/Lua ; trancher la
      divergence Round(AHK)/floor(Lua).
- [ ] Compléments de finding : `#15` part-2 (warn si fichier présent mais vide), `#72` throttle du
      flush métriques par frappe, `#9` empêcher l'écrasement du fichier corrompu, `#38` assertions
      `pause_state`/`errors_today`.

### ⏸️ Différé — écran / vraie machine

- [ ] Webview Windows → `WebViewHost` / `apps.manifest.json` (plan §4.1 — reload visuel requis).
- [ ] Câblage runtime des timings keylogger AHK (plan §4.2 — ordre de boot des `static` de classe).
- [ ] macOS `#63` : confirmer empiriquement le PID des échos `keyStrokes()` puis activer le gate
      d'écho (plan §5.4).
- [ ] Vérif daemon-only sur vrai Linux : luajit, evdev/ydotool/uinput/D-Bus/WebKitGTK/SNI, smoke
      complet, E2E réels, `install.sh` (Ubuntu 22.04/24.04, Fedora 38+, Debian 12, Arch) (plan §4.3).
- [ ] Smoke-tests Windows (hors CI) : boot du deferral idle-pass emoji/symboles ; sanity
      longueur/latence des prédictions après le token-cap.

## Règles & vérification (rappel)

Discipline complète et commandes exactes : voir **§0.2 / §0.3** du plan. En bref : jamais pusher ;
un commit = un finding + son test §5.9 prouvé rouge-avant ; pas d'`os.clock`/de refs de plan dans le
code ; AHK en UTF-8 BOM+CRLF édité via l'outil Edit.

> **Note capture AHK (piège récurrent) :** lancé depuis Git Bash, `AutoHotkey64.exe` (GUI) n'attache
> pas son stdout et écrit son TAP dans `%TEMP%\ergopti_test_results_<PID>.txt` (fichier **suffixé par
> PID**, pas le canonique). Nettoyer les anciens avant un run, lire le plus récent, et faire
> `git checkout -- static/ergopti_plus/windows/tests/test_config.ini` après (artefact muté).

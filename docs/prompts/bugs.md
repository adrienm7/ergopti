Tu es un senior developer expert en détection de bugs.
Analyse extrêmement finement mon code ergoptiplus à la recherche d'erreurs, de bugs, de race conditions, de comportement inattendus, d'optimisations de temps, etc.
On a plusieurs modules donc analyse module par module, mais ces modules interagissent ensemble donc regarde aussi tests end to end.
Prend le temps qu'il faut, tu dois me garantir que mon code est totalement bon et que aucune erreur ne sera détectée après ton passage.
Ton output doit être un md à la racine du projet avec toutes tes découvertes, comment il faudrait fix et quels tests de non régression implémenter.
